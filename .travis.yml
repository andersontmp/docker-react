sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t andersontmp/docker-react -f Dockerfile.dev .

scripts:
  - docker run -e CI=true andersontmp/docker-react npm run test

deploy:
  provider: elasticbeanstalk
  region: "us-east-2"
  app: "docker"
  env: "docker-env"
  bucket_name: "elasticbeanstalk-us-east-2-267387660376"
  bucket_path: "docker"
  on:
    branch: main
  access_key_id: "AKIAT4QML5BMJFB5DMGW"
  secret_access_key: "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"